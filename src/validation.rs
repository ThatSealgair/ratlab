/* Token validation for ratlab
 */ 
mod header;

// Library imports
use crates::header::*;

/* Main function for token validation.
 */
fn ratlab_validation(tokens: Vec<TokenType>) -> Vec<TokenType> {
    
}
